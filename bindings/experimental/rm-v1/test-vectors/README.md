# RM version 1 test vectors

No signed new-binding vector is installed yet. I1 will add generated target and
authority artifacts, authorized controller material, immutable catalog entries and
negative protection/mapping controls here. The unsigned regression predicates under
`testdata/regressions/` remain useful but are not signed reliance evidence.
