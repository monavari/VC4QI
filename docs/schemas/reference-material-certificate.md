# Reference Material Certificate Schema

Reference material certificates use `CredentialEvidenceReference` entries for
authorizing and supporting evidence. A typical recursive profile requires
operational-scope evidence, parent authority evidence, and a supporting
`ReferenceMaterialStudy`.

Use `digestMultibase` or `digestSRI` to bind referenced evidence.
