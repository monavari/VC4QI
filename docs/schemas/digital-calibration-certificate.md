# Digital Calibration Certificate Schema

The DCC schema uses standard VC 2.0 properties and accepts
`CredentialEvidenceReference` entries. No capability evidence is required by
default.

Verifier policy decides whether direct accreditation, capability evidence, legal
mandate, notification, scheme authorization, or another evidence form is
sufficient.
