# Scenario Tests

Root scenario tests exercise the v0.2 shared fixtures in `testdata/examples`.
The package-level TypeScript and Python suites contain the detailed edge,
policy, scope, digest, status, and presentation-query tests.
