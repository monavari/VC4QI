# TEST ONLY — NOT FOR PRODUCTION

All key material in this directory is used exclusively for deterministic test
execution. These keys are committed to the repository intentionally and are
**not secret**. Do not use them in any production or staging environment.

Keys are generated with a fixed seed and labelled `TEST ONLY` in each file.
